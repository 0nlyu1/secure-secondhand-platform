# Tiny Second-hand Shopping Platform 시스템 설계

## 1. 시스템 개요

본 시스템은 Python Flask와 SQLite를 사용하여 구현하는
소규모 중고거래 웹 애플리케이션이다.

사용자는 회원가입 후 상품을 등록하거나 조회할 수 있으며,
다른 사용자에게 메시지를 보내고 가상 포인트를 송금할 수 있다.

관리자는 사용자, 상품, 신고 내역을 조회하고
악성 사용자 계정과 부적절한 상품을 차단할 수 있다.

---

## 2. 기술 구성

| 구분 | 사용 기술 | 선택 이유 |
|---|---|---|
| 운영환경 | Ubuntu on WSL | 수업 환경과 동일한 Linux 개발환경 |
| 백엔드 | Python Flask | 구조가 단순하고 빠르게 웹 기능을 구현할 수 있음 |
| 데이터베이스 | SQLite | 별도의 DB 서버 없이 사용할 수 있음 |
| ORM | Flask-SQLAlchemy | SQL Injection 위험을 줄이고 DB 처리를 단순화 |
| 인증 | Flask 세션 | 로그인 사용자 상태 관리 |
| 비밀번호 | Werkzeug Password Hash | 비밀번호 평문 저장 방지 |
| 화면 | HTML, CSS, Jinja2 | Flask 기본 템플릿과 자동 이스케이프 사용 |
| 버전 관리 | Git, GitHub | 변경 내역 관리 및 과제 제출 |

---

## 3. 사용자 권한 설계

### 3.1 비회원

비회원은 다음 기능만 사용할 수 있다.

- 회원가입
- 로그인
- 상품 목록 조회
- 상품 상세 조회
- 상품 검색

비회원은 다음 기능을 사용할 수 없다.

- 상품 등록, 수정, 삭제
- 메시지 전송 및 조회
- 신고
- 포인트 송금
- 마이페이지
- 관리자 기능

### 3.2 일반 사용자

로그인한 일반 사용자는 다음 기능을 사용할 수 있다.

- 프로필 조회 및 소개글 수정
- 상품 등록
- 자신이 등록한 상품 수정 및 삭제
- 다른 사용자에게 메시지 전송
- 사용자 및 상품 신고
- 포인트 송금
- 송금 내역 확인

일반 사용자는 다른 사용자의 상품이나 계정 정보를 임의로 수정할 수 없다.

### 3.3 관리자

관리자는 일반 사용자 기능과 함께 다음 기능을 사용할 수 있다.

- 전체 사용자 조회
- 사용자 계정 정지 및 해제
- 전체 상품 조회
- 상품 차단 및 삭제
- 전체 신고 내역 조회
- 신고 처리 상태 변경

관리자 기능은 `is_admin` 값이 참인 사용자만 사용할 수 있다.

---

## 4. 웹페이지 설계

| 경로 | 페이지 | 접근 권한 | 주요 기능 |
|---|---|---|---|
| `/` | 메인 페이지 | 전체 | 상품 목록과 검색 |
| `/register` | 회원가입 | 비회원 | 계정 생성 |
| `/login` | 로그인 | 비회원 | 사용자 인증 |
| `/logout` | 로그아웃 | 로그인 사용자 | 세션 종료 |
| `/mypage` | 마이페이지 | 로그인 사용자 | 프로필, 포인트, 내 상품 확인 |
| `/profile/<id>` | 사용자 프로필 | 전체 | 사용자 소개와 등록 상품 확인 |
| `/products/new` | 상품 등록 | 로그인 사용자 | 새 상품 등록 |
| `/products/<id>` | 상품 상세 | 전체 | 상품 상세 정보 조회 |
| `/products/<id>/edit` | 상품 수정 | 판매자 또는 관리자 | 상품 정보 수정 |
| `/products/<id>/delete` | 상품 삭제 | 판매자 또는 관리자 | 상품 삭제 |
| `/messages` | 메시지 목록 | 로그인 사용자 | 대화 상대 목록 확인 |
| `/messages/<user_id>` | 1대1 메시지 | 로그인 사용자 | 메시지 송수신 |
| `/report/user/<id>` | 사용자 신고 | 로그인 사용자 | 사용자 신고 |
| `/report/product/<id>` | 상품 신고 | 로그인 사용자 | 상품 신고 |
| `/transfer` | 포인트 송금 | 로그인 사용자 | 다른 사용자에게 포인트 이전 |
| `/transactions` | 송금 내역 | 로그인 사용자 | 입출금 기록 확인 |
| `/admin` | 관리자 대시보드 | 관리자 | 전체 현황 확인 |
| `/admin/users` | 사용자 관리 | 관리자 | 사용자 정지 및 해제 |
| `/admin/products` | 상품 관리 | 관리자 | 상품 차단 및 삭제 |
| `/admin/reports` | 신고 관리 | 관리자 | 신고 내역 확인 및 처리 |

---

## 5. 데이터베이스 설계

### 5.1 User 테이블

사용자 계정 정보를 저장한다.

| 필드 | 자료형 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | Integer | PK | 사용자 고유 번호 |
| `username` | String(30) | UNIQUE, NOT NULL | 로그인 아이디 |
| `password_hash` | String(255) | NOT NULL | 해시된 비밀번호 |
| `bio` | String(300) | 기본값 빈 문자열 | 사용자 소개글 |
| `points` | Integer | 기본값 10000 | 보유 가상 포인트 |
| `is_admin` | Boolean | 기본값 False | 관리자 여부 |
| `is_suspended` | Boolean | 기본값 False | 계정 정지 여부 |
| `created_at` | DateTime | NOT NULL | 가입 시간 |

보안 설계:

- 비밀번호 원문은 저장하지 않는다.
- 아이디에는 UNIQUE 제약조건을 적용한다.
- 포인트는 음수가 되지 않도록 서버에서 검증한다.
- 관리자 여부는 사용자가 요청값으로 변경할 수 없도록 한다.

### 5.2 Product 테이블

상품 정보를 저장한다.

| 필드 | 자료형 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | Integer | PK | 상품 고유 번호 |
| `title` | String(100) | NOT NULL | 상품명 |
| `description` | Text | NOT NULL | 상품 설명 |
| `price` | Integer | NOT NULL | 상품 가격 |
| `seller_id` | Integer | FK, NOT NULL | 판매자 사용자 번호 |
| `is_blocked` | Boolean | 기본값 False | 상품 차단 여부 |
| `created_at` | DateTime | NOT NULL | 상품 등록 시간 |

보안 설계:

- 상품 가격은 0보다 큰 정수만 허용한다.
- 상품 수정과 삭제 시 판매자 또는 관리자 여부를 확인한다.
- 차단된 상품은 일반 상품 목록에서 제외한다.
- 판매자 번호는 요청값이 아니라 로그인 사용자 정보에서 가져온다.

### 5.3 Message 테이블

사용자 간 1대1 메시지를 저장한다.

| 필드 | 자료형 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | Integer | PK | 메시지 고유 번호 |
| `sender_id` | Integer | FK, NOT NULL | 발신자 번호 |
| `receiver_id` | Integer | FK, NOT NULL | 수신자 번호 |
| `content` | String(1000) | NOT NULL | 메시지 내용 |
| `created_at` | DateTime | NOT NULL | 발송 시간 |

보안 설계:

- 발신자는 로그인 사용자로 자동 지정한다.
- 발신자와 수신자만 메시지를 조회할 수 있다.
- 메시지 길이를 제한한다.
- Jinja2 자동 이스케이프를 사용하여 스크립트 실행을 방지한다.

### 5.4 Report 테이블

사용자 또는 상품에 대한 신고를 저장한다.

| 필드 | 자료형 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | Integer | PK | 신고 고유 번호 |
| `reporter_id` | Integer | FK, NOT NULL | 신고자 번호 |
| `target_type` | String(20) | NOT NULL | user 또는 product |
| `target_id` | Integer | NOT NULL | 신고 대상 번호 |
| `reason` | String(500) | NOT NULL | 신고 사유 |
| `status` | String(20) | 기본값 pending | 신고 처리 상태 |
| `created_at` | DateTime | NOT NULL | 신고 시간 |

보안 설계:

- 신고자는 로그인 사용자로 자동 지정한다.
- 신고 대상 유형은 허용 목록인 `user`, `product`만 받는다.
- 동일 사용자가 동일 대상을 중복 신고하지 못하도록 검사한다.
- 전체 신고 내역은 관리자만 조회할 수 있다.

### 5.5 Transaction 테이블

사용자 간 가상 포인트 송금 기록을 저장한다.

| 필드 | 자료형 | 제약조건 | 설명 |
|---|---|---|---|
| `id` | Integer | PK | 송금 고유 번호 |
| `sender_id` | Integer | FK, NOT NULL | 송금 사용자 번호 |
| `receiver_id` | Integer | FK, NOT NULL | 수신 사용자 번호 |
| `amount` | Integer | NOT NULL | 송금 포인트 |
| `created_at` | DateTime | NOT NULL | 송금 시간 |

보안 설계:

- 0 이하의 금액은 허용하지 않는다.
- 자신의 보유 포인트를 초과한 금액은 허용하지 않는다.
- 자기 자신에게 송금할 수 없다.
- 잔액 변경과 송금 기록 저장은 하나의 DB 트랜잭션으로 처리한다.
- 사용자가 전송한 sender_id는 신뢰하지 않고 로그인 세션을 사용한다.

---

## 6. 주요 처리 흐름

### 6.1 회원가입

1. 사용자가 아이디와 비밀번호를 입력한다.
2. 서버가 아이디와 비밀번호의 형식을 검증한다.
3. 서버가 아이디 중복 여부를 확인한다.
4. 비밀번호를 안전한 해시값으로 변환한다.
5. 사용자 정보를 데이터베이스에 저장한다.
6. 회원가입 성공 메시지를 출력한다.

### 6.2 로그인

1. 사용자가 아이디와 비밀번호를 입력한다.
2. 서버가 해당 아이디의 사용자를 조회한다.
3. 저장된 비밀번호 해시와 입력 비밀번호를 비교한다.
4. 계정 정지 여부를 확인한다.
5. 인증에 성공하면 사용자 번호를 세션에 저장한다.
6. 실패하면 구체적인 내부 정보 없이 로그인 실패 메시지를 출력한다.

### 6.3 상품 등록

1. 로그인 여부를 확인한다.
2. 사용자가 상품명, 설명, 가격을 입력한다.
3. 서버에서 입력값 길이와 가격 형식을 검증한다.
4. 판매자는 현재 로그인 사용자로 지정한다.
5. 상품 정보를 데이터베이스에 저장한다.

### 6.4 상품 수정 및 삭제

1. 로그인 여부를 확인한다.
2. 대상 상품을 데이터베이스에서 조회한다.
3. 로그인 사용자가 판매자 또는 관리자인지 확인한다.
4. 권한이 있는 경우에만 수정 또는 삭제한다.
5. 권한이 없으면 403 오류로 처리한다.

### 6.5 포인트 송금

1. 로그인 여부를 확인한다.
2. 수신 사용자와 송금 금액을 검증한다.
3. 자신에게 송금하는지 확인한다.
4. 보유 포인트가 충분한지 확인한다.
5. 송신자 포인트를 차감한다.
6. 수신자 포인트를 증가시킨다.
7. 송금 기록을 저장한다.
8. 모든 처리를 하나의 트랜잭션으로 반영한다.
9. 오류 발생 시 전체 작업을 취소한다.

### 6.6 관리자 처리

1. 로그인 여부를 확인한다.
2. 로그인 사용자의 관리자 권한을 확인한다.
3. 관리자만 사용자, 상품, 신고 정보를 조회한다.
4. 관리자가 사용자 정지 또는 상품 차단을 수행한다.
5. 일반 사용자의 직접 URL 접근은 403 오류로 차단한다.

---

## 7. 보안 위협과 설계 단계 대응

| 보안 위협 | 발생 가능 지점 | 설계 단계 대응 |
|---|---|---|
| SQL Injection | 로그인, 검색, 상품 조회 | SQLAlchemy ORM 사용 |
| 비밀번호 유출 | 사용자 DB | 비밀번호 해시 저장 |
| XSS | 상품 설명, 소개글, 메시지 | Jinja2 자동 이스케이프 및 입력 길이 제한 |
| 인증 우회 | 마이페이지, 메시지, 송금 | 로그인 확인 데코레이터 적용 |
| 권한 상승 | 관리자 페이지 | 관리자 권한 서버 측 검사 |
| IDOR | 상품 수정, 메시지 조회 | 대상 데이터의 소유자 여부 확인 |
| 포인트 조작 | 송금 요청 | 서버에서 잔액 및 금액 재검증 |
| 중복 송금 및 데이터 불일치 | 포인트 송금 | DB 트랜잭션 적용 |
| 비밀정보 공개 | GitHub | `.env`, DB 파일, 비밀키를 `.gitignore`에 등록 |
| 상세 오류정보 노출 | 서버 오류 | 운영 환경에서 디버그 모드 비활성화 |

---

## 8. 프로젝트 폴더 구조 계획

```text
whs-secure-coding/
├── app.py
├── models.py
├── requirements.txt
├── README.md
├── .gitignore
├── instance/
│   └── market.db
├── templates/
│   ├── base.html
│   ├── index.html
│   ├── register.html
│   ├── login.html
│   ├── mypage.html
│   ├── product_form.html
│   ├── product_detail.html
│   ├── messages.html
│   ├── transfer.html
│   └── admin.html
├── static/
│   └── style.css
├── docs/
├── evidence/
├── requirements.md
├── design.md
├── security.md
├── checklist.md
├── test.md
└── maintenance.md