# Tiny Second-hand Shopping Platform 유지보수 및 변경 내역

## 1. 문서 목적

본 문서는 Tiny Second-hand Shopping Platform을 개발하고 테스트하는 과정에서
발견한 문제점, 보안상 주의사항, 수정 내용 및 향후 개선 방향을 기록한 것이다.

기능을 단순히 구현하는 것에 그치지 않고,
테스트 결과에 따라 프로그램을 점검하고 유지보수하는 과정을 정리하였다.

---

## 2. 개발환경 구성 문제

### 문제 상황

Python 가상환경을 생성하는 과정에서
`python3 -m venv` 명령이 정상적으로 실행되지 않았다.

### 원인

Ubuntu 환경에 `python3.12-venv` 패키지가 설치되어 있지 않았다.

### 해결 방법

다음 명령으로 필요한 패키지를 설치하였다.

```bash
sudo apt install python3.12-venv
```

### 결과

프로젝트별 독립적인 Python 패키지 환경을 구성할 수 있었으며,
설치된 라이브러리는 `requirements.txt`에 기록하였다.

---

## 3. 비밀번호 평문 저장 위험 개선

### 발견한 문제

비밀번호를 데이터베이스에 그대로 저장할 경우,
데이터베이스가 유출되었을 때 사용자의 실제 비밀번호가 그대로 노출될 수 있다.

### 개선 내용

Werkzeug의 다음 함수를 사용하였다.

- `generate_password_hash()`
- `check_password_hash()`

회원가입 시 비밀번호를 해시값으로 변환하여 저장하고,
로그인 시 입력 비밀번호와 저장된 해시값을 비교하도록 수정하였다.

### 결과

데이터베이스에 사용자의 평문 비밀번호가 저장되지 않도록 하였다.

---

## 4. 클라이언트 검증만 사용하는 문제 개선

### 발견한 문제

상품 가격 입력란에 HTML의 min 속성을 적용하면
일반적인 브라우저 입력에서는 음수를 막을 수 있다.

그러나 개발자 도구나 직접 요청을 사용하면
브라우저의 검증을 우회할 가능성이 있다.

### 개선 내용

상품 등록 및 수정 요청을 처리하는 서버 코드에서도
가격을 정수로 변환한 뒤 다음 조건을 검사하도록 하였다.

가격이 숫자인지 확인
가격이 1 이상인지 확인
비정상적으로 큰 값인지 확인

회원가입, 소개글, 메시지, 신고 사유, 송금 금액에도
서버 측 길이 및 형식 검증을 적용하였다.

### 결과

브라우저 검증 우회 여부와 관계없이
서버가 잘못된 입력을 최종적으로 차단하도록 개선하였다.

---

## 5. 타인 데이터 수정 가능성 개선

### 발견한 문제

상품 수정 및 삭제 URL에 상품 ID가 포함되므로,
사용자가 주소의 숫자를 변경하여 타인의 상품에 접근할 가능성이 있다.

### 개선 내용

상품을 수정하거나 삭제할 때 다음 조건을 서버에서 검사하였다.

현재 사용자가 로그인했는지 확인
상품 등록자 ID와 로그인 사용자 ID 비교
관리자 권한 여부 확인

화면에서 버튼을 숨기는 것만으로 권한을 처리하지 않고,
요청이 서버에 도착했을 때 다시 권한을 검증하였다.

### 결과

일반 사용자가 URL을 직접 변경하더라도
타인의 상품을 임의로 수정하거나 삭제할 수 없도록 하였다.

---

## 6. 메시지 발신자 위조 위험 개선

### 발견한 문제

메시지 발신자 정보를 사용자가 보낸 요청값에서 가져오면,
공격자가 다른 사용자의 ID를 넣어 발신자를 위조할 수 있다.

### 개선 내용

메시지의 발신자 ID는 폼 입력값에서 받지 않고
로그인 세션에 저장된 사용자 ID를 사용하도록 하였다.

수신자 존재 여부와 자기 자신에게 보내는지 여부도 검사하였다.

### 결과

사용자가 다른 사람을 가장하여 메시지를 전송하는 위험을 줄였다.

---

## 7. 중복 신고 및 자기 신고 문제 개선

### 발견한 문제

같은 사용자가 특정 사용자나 상품을 반복적으로 신고하면
신고 데이터가 불필요하게 증가할 수 있다.

또한 자기 자신이나 자신이 등록한 상품을 신고하는 것은
정상적인 신고 흐름이 아니다.

### 개선 내용

신고 요청을 저장하기 전에 다음 항목을 검사하였다.

- 자기 자신 신고 여부
- 자기 상품 신고 여부
- 동일 대상에 대한 기존 신고 여부
- 신고 사유 길이

### 결과

중복 신고와 자기 신고가 차단되고,
관리자가 확인할 수 있는 유효한 신고만 저장되도록 하였다.

---

## 8. 관리자 페이지 접근제어 개선

### 발견한 문제

관리자 메뉴를 일반 사용자 화면에서 숨기는 것만으로는
관리자 URL 직접 접근을 막을 수 없다.

### 개선 내용

관리자 관련 모든 요청에서 다음 사항을 서버에서 확인하였다.

- 로그인 여부
- 로그인 사용자의 관리자 권한 여부

일반 사용자가 /admin 주소를 직접 입력해도
관리자 기능을 실행할 수 없도록 하였다.

또한 관리자가 자신의 계정이나 다른 관리자 계정을
임의로 정지하지 못하도록 제한하였다.

### 결과

일반 사용자와 관리자의 권한을 구분하고,
중요 관리 기능에 대한 접근제어를 적용하였다.

## 9. 포인트 송금 무결성 개선

### 발견한 문제

송신자 포인트 차감과 수신자 포인트 증가가
서로 다른 시점에 저장되면 다음 문제가 발생할 수 있다.

- 송신자 포인트만 차감됨
- 수신자 포인트만 증가함
- 송금 기록이 저장되지 않음

### 개선 내용

다음 작업을 하나의 데이터베이스 처리 흐름으로 구성하였다.

1. 송신자 포인트 차감
2. 수신자 포인트 증가
3. 송금 기록 생성
4. 하나의 commit()으로 저장

오류가 발생하면 rollback()을 수행하도록 하였다.

또한 다음 송금을 차단하였다.

- 자기 자신에게 송금
- 존재하지 않는 사용자에게 송금
- 정지 사용자에게 송금
- 0원 또는 음수 송금
- 보유 포인트보다 큰 금액 송금

### 결과

송금 과정의 데이터 일관성을 높이고
비정상적인 포인트 증가 및 감소를 방지하였다.

---

## 10. SQL Injection 테스트 및 점검

### 테스트 내용

로그인 아이디와 상품 검색창에 다음 문자열을 입력하였다.

```bash
' OR 1=1 --
```

### 테스트 결과
- 로그인 인증을 우회하지 못하였다.
- 일반적인 로그인 실패 메시지가 출력되었다.
- 상품 검색에서 서버 오류가 발생하지 않았다.
- 전체 상품이 강제로 노출되지 않았다.
- 입력 문자열은 일반 검색어로 처리되었다.

### 적용된 대응

SQLAlchemy ORM을 사용하고,
사용자 입력값을 SQL 문자열에 직접 이어 붙이지 않았다.

### 결과

실행한 테스트 범위에서는 SQL Injection으로 인한
인증 우회나 데이터 노출이 발생하지 않았다.

---

## 11. 저장형 XSS 테스트 및 점검

### 테스트 내용

마이페이지 소개글에 다음 문자열을 저장하였다.

```html
<script>alert(1)</script>
```

### 테스트 결과

브라우저 경고창이 실행되지 않았다.
입력 문자열이 화면에 텍스트로 표시되었다.
스크립트가 HTML 코드로 실행되지 않았다.

### 적용된 대응

Jinja2의 기본 자동 이스케이프 기능을 사용하였으며,
사용자 입력을 출력할 때 safe 필터를 사용하지 않았다.

### 결과

테스트한 소개글 출력 위치에서
기본적인 저장형 XSS 방어가 정상적으로 작동함을 확인하였다.

---

## 12. 현재 확인된 잔여 보안 위험

현재 과제 버전에는 다음 보안 기능이 적용되지 않았거나
추가적인 개선이 필요하다.

### CSRF 방어 미적용

상태를 변경하는 POST 요청에 CSRF 토큰이 적용되지 않았다.

향후 Flask-WTF의 CSRFProtect를 적용하여
외부 사이트에서 사용자의 권한을 악용한 요청을 보내는 것을 방지해야 한다.

### SECRET_KEY 코드 내부 저장

개발 편의를 위해 Flask의 SECRET_KEY가 코드 내부에 존재할 수 있다.

실제 배포 환경에서는 환경변수 또는 별도의 비밀 관리 방법으로 이전해야 한다.

### 로그인 시도 제한 미적용

반복적인 비밀번호 입력 시도를 제한하지 않았다.

향후 Flask-Limiter 또는 일정 횟수 이상 실패 시
일시적으로 로그인을 제한하는 기능을 적용할 수 있다.

### HTTPS 미적용

현재는 로컬 개발환경의 HTTP를 사용한다.

실제 서비스에서는 TLS 인증서를 적용하여
로그인 정보와 세션 데이터가 암호화된 통신으로 전달되도록 해야 한다.

### 관리자 감사 로그 미적용

사용자 정지, 상품 차단, 신고 처리 등의 관리자 작업을
별도의 감사 로그 테이블에 저장하지 않는다.

향후 관리자 ID, 작업 종류, 대상, 처리 시각을 기록하는 기능이 필요하다.

### 동시 송금 요청 검증 부족

현재 송금은 하나의 commit과 rollback 구조로 처리하지만,
여러 송금 요청이 동시에 발생하는 상황은 별도로 검증하지 않았다.

실제 운영환경에서는 데이터베이스 잠금,
원자적 조건부 갱신 또는 트랜잭션 격리 수준을 검토해야 한다.

---

## 13. 향후 유지보수 계획

### 단기 개선

- SECRET_KEY를 환경변수로 이동
- .gitignore에 데이터베이스와 가상환경 추가
- README에 설치 및 실행 방법 작성
- 테스트 계정과 불필요한 데이터를 정리
- 메시지와 상품 입력 위치의 XSS 테스트 추가

### 중기 개선

- CSRFProtect 적용
- 로그인 시도 횟수 제한
- 관리자 감사 로그 추가
- 비밀번호 정책 강화
- 세션 쿠키 보안 설정 강화
- 예외 처리 및 사용자 오류 메시지 개선

### 장기 개선

- HTTPS 적용
- 운영용 WSGI 서버 사용
- 데이터베이스 백업 및 복구 기능 추가
- 동시 송금 경쟁 상태 대응
- 상품 이미지 업로드 및 파일 검증
- 자동화된 단위 테스트 및 통합 테스트 구축

---

14. 유지보수 결과 요약

개발 과정에서 인증, 권한 검사, 입력값 검증,
비밀번호 저장, 상품 소유권, 관리자 권한,
신고 중복, 메시지 발신자, 포인트 송금 무결성을 중점적으로 점검하였다.

또한 SQL Injection과 저장형 XSS 공격 문자열을 직접 입력하여
기본적인 방어 기능이 정상적으로 동작하는지 확인하였다.

현재 과제에서 요구한 핵심 기능은 정상적으로 작동하지만,
CSRF 방어와 운영환경 보안 설정 등은 향후 개선사항으로 남아 있다.

